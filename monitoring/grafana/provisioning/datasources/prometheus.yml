apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      # Matches scrape_interval in monitoring/prometheus.yml, so rate()
      # windows are never shorter than two scrapes.
      timeInterval: 15s
